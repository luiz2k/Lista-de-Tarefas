import { HomePage } from "@/modules/dashboard/pages/Home";

import type { Metadata } from "next";

export const metadata: Metadata = {
	title: "Lista de Tarefas | Painel",
	description: "",
};

export default HomePage;
